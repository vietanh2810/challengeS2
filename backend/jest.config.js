module.exports = {
    testEnvironment: 'node', // Specifies the testing environment (e.g., 'jsdom' for browser-like testing)
    testMatch: ['**/__tests__/*.test.js'], // Specifies the pattern for test files
    verbose: true,
    forceExit: true,
    // clearMocks: true, 
  };